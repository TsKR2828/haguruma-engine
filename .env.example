# 歯車引擎 — 環境變數範例
# 複製此檔為 .env 後填入實際值

# 部署目標（vercel / github-pages / local）
DEPLOY_TARGET=local

# Google Fonts API Key（可選，用於自託管字型）
# GOOGLE_FONTS_API_KEY=

# 開發模式（啟用除錯面板、跳過打字動畫等）
DEV_MODE=false

# 存檔系統偏好（persistent / session / memory）
SAVE_BACKEND=persistent
